version: "3.6"
services:
  server:
    container_name: nestjs
    build: .
    ports:
      - 3000:3000
    depends_on:
      - mongodb
    
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: {}
  